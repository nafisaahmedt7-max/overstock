"use client";
import { FormEvent,useState } from "react";
import { createClient } from "@/lib/supabase/client";
export default function PortalLogin(){const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);const f=new FormData(e.currentTarget);const{error}=await createClient().auth.signInWithOtp({email:String(f.get("email")),options:{emailRedirectTo:`${window.location.origin}/auth/callback?next=/portal`,shouldCreateUser:true}});setMessage(error?.message||"Check your email for the secure access link.");setBusy(false)}return <main className="admin-login"><form className="admin-panel" onSubmit={submit}><p className="eyebrow">OVERSTOCK COLLECTIVE / PARTNERS</p><h1>PORTAL LOGIN</h1><label>Approved email<input type="email" name="email" required autoComplete="email"/></label><button className="admin-primary" disabled={busy}>{busy?"SENDING…":"EMAIL ACCESS LINK"}</button>{message&&<p className="admin-error" role="status">{message}</p>}</form></main>}
